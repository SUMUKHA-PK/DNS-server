package mit

import (
	"fmt"
)

func Mit() {
	fmt.Printf("I am the mit authoritative server!\n")

	// Starting the server
	// link, err := net.Listen("tcp", "127.0.2.5:12345")
	// if err != nil {
	// 	fmt.Print(err)
	// 	fmt.Printf("\n2\n")
	// }
}
